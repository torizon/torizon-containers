services:
  qt6-wayland-tests:
    image: $REGISTRY/${REGISTRY_NAMESPACE}/qt6-wayland-tests:${IMAGE_TAG}
    container_name: qt6-wayland-tests
    privileged: true
    stdin_open: true
    tty: true
    command: bash
    volumes:
      - /dev:/dev
      - /run/udev:/run/udev
      - /tmp:/tmp
