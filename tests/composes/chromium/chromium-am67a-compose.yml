# description: launches the Weston Compositor with shell and then Chromium opening the Toradex Homepage
services:
  weston:
    network_mode: host
    cap_add:
      - CAP_SYS_TTY_CONFIG
    volumes:
      - '/dev:/dev'
      - '/tmp:/tmp'
      - '/run/udev/:/run/udev/'
    device_cgroup_rules:
      - 'c 4:* rmw'
      - 'c 13:* rmw'
      - 'c 226:* rmw'
    image: '$REGISTRY/torizon/weston-am67a:stable-rc'
    command: '--developer'

  chromium:
    command: https://www.toradex.com
    depends_on:
      weston:
        condition: service_healthy
    device_cgroup_rules:
      - 'c 226:* rmw'
    image: $REGISTRY/torizon/chromium-am67a:stable-rc
    security_opt:
    - seccomp:unconfined
    shm_size: 256mb
    volumes:
      - '/tmp:/tmp'
      - '/var/run/dbus:/var/run/dbus'
      - '/dev/dri:/dev/dri'
