ARG BASE_NAME=
ARG IMAGE_TAG=
ARG BASE_NAME_DEBUG=
ARG DOCKER_REGISTRY=torizon

FROM ${DOCKER_REGISTRY}/${BASE_NAME_DEBUG}:${IMAGE_TAG} AS dotnet

FROM ${DOCKER_REGISTRY}/${BASE_NAME}:${IMAGE_TAG} AS base

COPY --from=dotnet /vsdbg /vsdbg
